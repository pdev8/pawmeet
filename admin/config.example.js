// Copy this file to `admin/config.js` and fill in your project's values.
// The anon key is public-by-design (RLS protects the data), but we keep it out
// of git to match the app's .env policy — `admin/config.js` is gitignored.
window.PAWK_ADMIN_CONFIG = {
  supabaseUrl: 'https://YOUR-PROJECT.supabase.co',
  supabaseAnonKey: 'YOUR-ANON-KEY',
};
